MEMORY {
	RAM:	start = $0000,	size = $4000;
	ROM:	start = $8000,	size = $7ffa, fill = yes, file = %O;
	VROM:	start = $fffa,	size = $0006, fill = yes, file = %O;
}

SEGMENTS {
	CODE:	load	= ROM,	type = ro;
	RODATA:	load	= ROM,	type = ro;
	VECTS:	load	= VROM, type = ro;
}
